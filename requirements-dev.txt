# Apex AI — development dependencies (install on top of requirements.txt)
#
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0,<10
packaging>=24,<27       # parse and validate requirement manifests in tests
# Starlette >=1.2 TestClient prefers httpx2; plain httpx is deprecated there.
httpx2>=2.0,<3
fpdf2>=2.7,<3         # regenerate committed PDF fixtures
# Generate scripts/make_tiny_gguf.py's structural provider smoke-test artifact.
gguf>=0.19,<0.20
numpy>=1.26,<3
ruff>=0.9,<1             # linter used to verify every phase; now pinned instead of ambient
