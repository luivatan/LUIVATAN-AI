# Direct runtime dependencies for LUIVATAN AI.
# Install an optional LLM backend separately when needed.
chromadb==1.5.9
gradio==6.15.1
pypdf==6.12.2
python-dotenv==1.2.2
requests==2.34.2
sentence-transformers==5.5.1

# Optional providers:
# llama-cpp-python==0.3.23
# transformers==5.9.0
# pytesseract==0.3.13  # OCR integration (system tesseract is also required)
