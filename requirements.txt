# Apex AI — core dependencies (offline-first RAG assistant)
#
# Install:            pip install -r requirements.txt
# Development:        pip install -r requirements-dev.txt
# GPU acceleration:   see requirements-gpu.txt
#
# The previous requirements.txt was a raw `pip freeze` dump (150 pins,
# machine-local packages like `my-custom-module`, CUDA wheels, unused
# langchain/langgraph/kubernetes) that could not install on a new machine.
# These are the packages the application actually imports.

# --- document processing -------------------------------------------------
pypdf>=4.0,<7            # PDF text extraction with page numbers

# --- embeddings -----------------------------------------------------------
sentence-transformers>=3.0,<6   # all-MiniLM-L6-v2 by default (downloads once, then offline)

# --- vector database --------------------------------------------------------
chromadb>=1.0,<2         # persistent local vector store

# --- retrieval ----------------------------------------------------------------
rank-bm25>=0.2.2         # keyword/BM25 stage of hybrid retrieval

# --- LLM inference ------------------------------------------------------------
llama-cpp-python>=0.3,<0.4   # local GGUF inference (compiles on first install; see README)
requests>=2.31           # Ollama / OpenAI-compatible HTTP providers

# --- UI / API ---------------------------------------------------------------
gradio>=5.0,<7           # main user interface
fastapi>=0.110           # optional JSON API (already a Gradio dependency)
uvicorn>=0.29            # runs the FastAPI app
pydantic>=2.6            # request/response models

# --- configuration & utilities ----------------------------------------------
python-dotenv>=1.0       # .env file loading
numpy>=1.26
